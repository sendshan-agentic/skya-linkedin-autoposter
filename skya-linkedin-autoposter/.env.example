# Copy this to .env.local for the ONE-TIME local token setup
# (scripts/get-linkedin-token.mjs). Do not commit .env.local.
# In GitHub Actions, these same names are set as repo Secrets instead.

GEMINI_API_KEY=
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_ACCESS_TOKEN=
LINKEDIN_REFRESH_TOKEN=
